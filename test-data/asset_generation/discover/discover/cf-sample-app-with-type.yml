---
name: app-with-type
metadata:
  annotations:
    contact: "bob@example.com jane@example.com"
  labels:
    sensitive: true
disk_quota: 512M
memory: 500M
timeout: 10
docker:
  image: myregistry/myapp:latest
  username: docker-registry-user
type: web
